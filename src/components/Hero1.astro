---
const {
  title = "Capture Your Moments",
  subtitle = "Premium photography services for events, portraits, and more.",
  ctaText = "Book a Session",
  ctaLink = "/contact",
  image = "/images/Blessi-Purple-Flowers.jpg"
} = Astro.props;
---

<section class="relative bg-gray-100">
  <div class="max-w-7xl mx-auto px-6 py-24 grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
    <div>
      <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 mb-6">
        {title}
      </h1>
      <p class="text-lg text-gray-700 mb-8">{subtitle}</p>
      <a href={ctaLink} class="inline-block bg-blue-600 text-white px-6 py-3 rounded-xl shadow hover:bg-blue-700 transition">
        {ctaText}
      </a>
    </div>
    <div>
      <img src={image} alt="Hero Image" class="rounded-2xl shadow-lg w-full object-cover h-80 md:h-[400px]" />
    </div>
  </div>
</section>
